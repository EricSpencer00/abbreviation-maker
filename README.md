# /meaning

A tiny phrase and abbreviation generator. Enter an abbreviation, a meaning, or both. `/meaning` returns five possible interpretations and five alternatives.

## Development

```sh
npm install
npm run dev
```

## Build

```sh
npm run build
```

The app is client-only in v1. It has no accounts, analytics, API keys, external requests, or stored user input.
